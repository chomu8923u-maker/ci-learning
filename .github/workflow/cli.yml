name: First CI

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/Checkout@v4

      - name: Run python file
        uses: python wrong_hello.py